# === 基础服务 ===
# 所有服务访问的数据库（API、formatter、scheduler、AI 任务）
DATABASE_URL=postgresql+psycopg://medpol:medpol@localhost:5432/medpol
# Celery 的 broker/backend（formatter/ai worker、scheduler）
REDIS_URL=redis://localhost:6379/0

# === AI 提供方（ai_processor 与 ai_chat 共用）===
# 当 provider=openai 时的凭据与默认模型，可对接 OpenAI/OpenRouter/GPTOSS
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 当 provider=deepseek 时的凭据与默认模型
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 全局 LLM 调用的优先级
AI_PRIMARY=openai
AI_FALLBACK=deepseek

# 对话专用模型
# AI_CHAT_MODEL：/v1/ai/chat 主回答（Stage-B）；留空则退回 OPENAI_MODEL
# AI_ROUTER_MODEL：能力判定（Stage-A）小模型；留空则复用 AI_CHAT_MODEL
AI_CHAT_MODEL=gpt-4o-mini
AI_ROUTER_MODEL=

# === 格式化配置 ===
# 去重状态文件（formatter worker 使用）；重跑全量前可清空
FORMATTER_SEEN_PATH=sample_data/state/formatter_seen.json

# === 财务数据同步（scripts/sync_finance_data.py）===
# 拉取财务数据的接口信息
FINANCE_API_BASE_URL=http://ailianhuan.xyz:8333
FINANCE_API_TIMEOUT=30
FINANCE_DATA_ENDPOINT=http://ailianhuan.xyz:8333/financeDate/dataList

# === Admin Portal / API Gateway ===
# Admin Portal 请求后端的基地址
VITE_API_BASE_URL=http://localhost:8000
# Admin Portal 日志查看读取的本地文件
LOG_FILE_PATH=test.log
# CORS 允许源（逗号分隔）；留空则后端默认 allow *
ADMIN_PORTAL_ORIGINS=http://localhost:4173,http://127.0.0.1:4173
